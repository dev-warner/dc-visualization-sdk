{
  "name": "dc-visualization-sdk",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "build": "lerna exec -- yarn run build",
    "build:lib": "lerna run build:watch --stream --parallel --scope=@dc-visualization-sdk/cli, @dc-visualization-sdk/core",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
